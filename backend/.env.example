# Server
PORT=3001
NODE_ENV=development

# Auth — change these before any deployment!
APP_USERNAME=admin
APP_PASSWORD=change-me-before-deploy
JWT_SECRET=change-me-before-deploy

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:5173

# Logging
LOG_LEVEL=info
