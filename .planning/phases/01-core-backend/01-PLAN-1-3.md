---
plan: "1.3"
title: "Anti-Detection Engine"
phase: 1
wave: 2
depends_on: ["1.1"]
autonomous: true
requirements_addressed: [ANT-01, ANT-02, ANT-03, ANT-04, ANT-05, ANT-06, ANT-07, ANT-08, ANT-09, ANT-10, ANT-11, ANT-12, WRN-01, WRN-02, WRN-03, WRN-04, WRN-05]
files_modified:
  - backend/src/utils/humanizer.ts
  - backend/src/utils/delays.ts
---

# Plan 1.3: Anti-Detection Engine

## Objective

Реалізувати повну систему антидетекту: Gaussian-розподіл затримок, Bezier-рух миші, посимвольний друк з помилками, людський скролінг, денні ліміти з трекером. Це серце захисту від бану LinkedIn.

## must_haves

```yaml
truths:
  - backend/src/utils/delays.ts містить SAFE_LIMITS константу з точними значеннями з ТЗ
  - backend/src/utils/delays.ts містить DailyLimitChecker що перевіряє limits проти daily_tracker
  - backend/src/utils/humanizer.ts містить gaussianDelay (Gaussian distribution, не Math.random uniform)
  - backend/src/utils/humanizer.ts містить humanMouseMove (Bezier-криві, 5-10 проміжних точок)
  - backend/src/utils/humanizer.ts містить humanType (посимвольно, 50-200ms, іноді помилка)
  - backend/src/utils/humanizer.ts містить humanScroll (скролінг вниз-вгору перед дією)
  - isWithinWorkingHours() повертає false о 3:00 і true о 10:00 у робочий день
```

## Tasks

<task id="1.3.1">
<title>SAFE_LIMITS + Daily Limit Checker</title>

<read_first>
- CLAUDE.md (секція Critical: Anti-Detection Rules → Денні ліміти)
- .planning/REQUIREMENTS.md (ANT-08, ANT-09, ANT-10, ANT-11, ANT-12)
</read_first>

<action>
Створити `backend/src/utils/delays.ts`:

```typescript
import { getTodayTracker } from '../services/storage';

// Безпечні ліміти LinkedIn 2026
// НІКОЛИ не збільшувати connectionRequestsPerDay понад 20 без тестування на свіжому акаунті
export const SAFE_LIMITS = {
  connectionRequestsPerDay: 20,
  messagesPerDay: 15,
  profileVisitsPerDay: 80,
  searchesPerDay: 20,

  // Затримки в мілісекундах
  betweenActions: { min: 15_000, max: 45_000 },   // 15-45 сек між окремими діями
  betweenLeads: { min: 60_000, max: 180_000 },    // 1-3 хв між лідами
  betweenSessions: { min: 3_600_000, max: 7_200_000 }, // 1-2 год між сесіями

  // Робочий розклад
  workingHours: { start: 9, end: 18 },             // 09:00 - 18:00
  workingDays: [1, 2, 3, 4, 5],                    // пн (1) - пт (5)
} as const;

// Перевірка чи зараз робочий час (з урахуванням timezone кампанії)
export function isWithinWorkingHours(timezone = 'America/New_York'): boolean {
  const now = new Date();
  const formatter = new Intl.DateTimeFormat('en-US', {
    timeZone: timezone,
    hour: 'numeric',
    hour12: false,
    weekday: 'short',
  });

  const parts = formatter.formatToParts(now);
  const hour = parseInt(parts.find(p => p.type === 'hour')?.value || '0', 10);
  const weekday = parts.find(p => p.type === 'weekday')?.value || '';

  const weekdayMap: Record<string, number> = {
    Sun: 0, Mon: 1, Tue: 2, Wed: 3, Thu: 4, Fri: 5, Sat: 6,
  };
  const dayNum = weekdayMap[weekday] ?? 0;

  const isWorkingDay = SAFE_LIMITS.workingDays.includes(dayNum as 1 | 2 | 3 | 4 | 5);
  const isWorkingHour = hour >= SAFE_LIMITS.workingHours.start && hour < SAFE_LIMITS.workingHours.end;

  return isWorkingDay && isWorkingHour;
}

export interface DailyLimits {
  connectionsRemaining: number;
  messagesRemaining: number;
  profileVisitsRemaining: number;
  canSendConnection: boolean;
  canSendMessage: boolean;
  canVisitProfile: boolean;
}

// Отримати поточний стан денних лімітів
export function getDailyLimits(): DailyLimits {
  const tracker = getTodayTracker() as {
    connections_sent: number;
    messages_sent: number;
    profiles_visited: number;
  };

  const connectionsRemaining = Math.max(0, SAFE_LIMITS.connectionRequestsPerDay - tracker.connections_sent);
  const messagesRemaining = Math.max(0, SAFE_LIMITS.messagesPerDay - tracker.messages_sent);
  const profileVisitsRemaining = Math.max(0, SAFE_LIMITS.profileVisitsPerDay - tracker.profiles_visited);

  return {
    connectionsRemaining,
    messagesRemaining,
    profileVisitsRemaining,
    canSendConnection: connectionsRemaining > 0,
    canSendMessage: messagesRemaining > 0,
    canVisitProfile: profileVisitsRemaining > 0,
  };
}

// Перевірка чи можна виконати конкретну дію
export function canPerformAction(action: 'connection' | 'message' | 'visit'): boolean {
  const limits = getDailyLimits();
  switch (action) {
    case 'connection': return limits.canSendConnection;
    case 'message': return limits.canSendMessage;
    case 'visit': return limits.canVisitProfile;
  }
}
```
</action>

<acceptance_criteria>
- backend/src/utils/delays.ts містить `connectionRequestsPerDay: 20`
- Містить `messagesPerDay: 15`
- Містить `betweenLeads: { min: 60_000, max: 180_000 }`
- Містить `betweenSessions: { min: 3_600_000, max: 7_200_000 }`
- Містить `export function isWithinWorkingHours`
- Містить `export function getDailyLimits`
- Містить `export function canPerformAction`
</acceptance_criteria>
</task>

<task id="1.3.2">
<title>Humanizer: Gaussian delay + mouse movement</title>

<read_first>
- CLAUDE.md (секція Critical: Anti-Detection Rules → Humanizer принципи)
- .planning/REQUIREMENTS.md (ANT-01, ANT-02, ANT-05)
</read_first>

<action>
Створити `backend/src/utils/humanizer.ts` (частина 1 — delay і mouse):

```typescript
import { Page, ElementHandle } from 'playwright';

// --- Gaussian Distribution Delay ---
// Box-Muller transform для нормального розподілу
// Людина робить паузу "близько N секунд" з відхиленням, а не рівно від min до max
function gaussianRandom(mean: number, stdDev: number): number {
  let u = 0, v = 0;
  while (u === 0) u = Math.random();
  while (v === 0) v = Math.random();
  return mean + stdDev * Math.sqrt(-2.0 * Math.log(u)) * Math.cos(2.0 * Math.PI * v);
}

export async function gaussianDelay(minMs: number, maxMs: number): Promise<void> {
  const mean = (minMs + maxMs) / 2;
  const stdDev = (maxMs - minMs) / 6; // 99.7% значень в межах [min, max]
  const delay = Math.max(minMs, Math.min(maxMs, gaussianRandom(mean, stdDev)));
  await new Promise(r => setTimeout(r, Math.round(delay)));
}

// --- Bezier Curve Mouse Movement ---
// Рух по кривій Безьє через 5-10 проміжних точок — імітація природного руху миші

interface Point { x: number; y: number }

function bezierPoint(t: number, points: Point[]): Point {
  if (points.length === 1) return points[0];
  const newPoints: Point[] = [];
  for (let i = 0; i < points.length - 1; i++) {
    newPoints.push({
      x: (1 - t) * points[i].x + t * points[i + 1].x,
      y: (1 - t) * points[i].y + t * points[i + 1].y,
    });
  }
  return bezierPoint(t, newPoints);
}

function generateBezierPath(from: Point, to: Point, numControlPoints = 3): Point[] {
  const controls: Point[] = [from];
  for (let i = 0; i < numControlPoints; i++) {
    controls.push({
      x: from.x + (to.x - from.x) * (i + 1) / (numControlPoints + 1) + (Math.random() - 0.5) * 100,
      y: from.y + (to.y - from.y) * (i + 1) / (numControlPoints + 1) + (Math.random() - 0.5) * 100,
    });
  }
  controls.push(to);
  return controls;
}

export async function humanMouseMove(page: Page, target: ElementHandle): Promise<void> {
  const box = await target.boundingBox();
  if (!box) return;

  const targetX = box.x + box.width / 2 + (Math.random() - 0.5) * 10;
  const targetY = box.y + box.height / 2 + (Math.random() - 0.5) * 10;

  // Поточна позиція миші (або центр сторінки якщо невідома)
  const viewport = page.viewportSize() || { width: 1366, height: 768 };
  const fromX = viewport.width / 2 + (Math.random() - 0.5) * 200;
  const fromY = viewport.height / 2 + (Math.random() - 0.5) * 200;

  const numSteps = 5 + Math.floor(Math.random() * 6); // 5-10 кроків
  const controlPoints = generateBezierPath({ x: fromX, y: fromY }, { x: targetX, y: targetY });

  for (let i = 0; i <= numSteps; i++) {
    const t = i / numSteps;
    const point = bezierPoint(t, controlPoints);
    await page.mouse.move(point.x, point.y);
    await new Promise(r => setTimeout(r, 10 + Math.random() * 30)); // мікро-затримка між кроками
  }
}
```
</action>

<acceptance_criteria>
- backend/src/utils/humanizer.ts містить `function gaussianRandom`
- Містить `Box-Muller` в коментарі або реалізацію через `Math.sqrt(-2.0 * Math.log(u))`
- Містить `export async function gaussianDelay`
- Містить `function bezierPoint`
- Містить `function generateBezierPath`
- Містить `export async function humanMouseMove`
- `humanMouseMove` викликає `page.mouse.move` в циклі (не один раз)
</acceptance_criteria>
</task>

<task id="1.3.3">
<title>Humanizer: human typing + scrolling</title>

<read_first>
- backend/src/utils/humanizer.ts (щойно створений, додаємо функції)
- CLAUDE.md (секція Critical: Anti-Detection Rules → ANT-03, ANT-04, ANT-05)
- .planning/REQUIREMENTS.md (ANT-03, ANT-04, ANT-05)
</read_first>

<action>
Додати до `backend/src/utils/humanizer.ts` (append після існуючого коду):

```typescript
// --- Human Typing ---
// Посимвольний ввід з варіативною швидкістю та іноді помилками

export async function humanType(page: Page, element: ElementHandle, text: string): Promise<void> {
  await element.click();
  await gaussianDelay(200, 500);

  for (let i = 0; i < text.length; i++) {
    const char = text[i];

    // Іноді робити помилку (~3% шанс для довгих текстів, тільки для буквено-цифрових символів)
    const shouldMistake = text.length > 20 && Math.random() < 0.03 && /[a-zA-Z]/.test(char);

    if (shouldMistake) {
      const mistakeChar = String.fromCharCode(char.charCodeAt(0) + (Math.random() < 0.5 ? 1 : -1));
      await page.keyboard.type(mistakeChar);
      await gaussianDelay(100, 300);
      await page.keyboard.press('Backspace');
      await gaussianDelay(150, 400);
    }

    await page.keyboard.type(char);

    // Варіативна швидкість: цифри і розділові знаки трохи повільніше
    const baseDelay = /[0-9.,!?]/.test(char) ? 100 : 60;
    const maxDelay = /[0-9.,!?]/.test(char) ? 250 : 180;
    await gaussianDelay(baseDelay, maxDelay);

    // Іноді пауза між словами (після пробілу)
    if (char === ' ' && Math.random() < 0.2) {
      await gaussianDelay(200, 600);
    }
  }
}

// --- Human Scroll ---
// Скролінг вниз-вгору перед кожною дією — імітація читання сторінки

export async function humanScroll(page: Page): Promise<void> {
  const numScrolls = 2 + Math.floor(Math.random() * 4); // 2-5 скролів

  for (let i = 0; i < numScrolls; i++) {
    const scrollDown = Math.random() < 0.8; // переважно вниз
    const amount = 100 + Math.random() * 300;

    await page.mouse.wheel(0, scrollDown ? amount : -amount / 2);
    await gaussianDelay(300, 800);
  }

  // Іноді повернутись назад вгору трохи
  if (Math.random() < 0.4) {
    await page.mouse.wheel(0, -(50 + Math.random() * 150));
    await gaussianDelay(200, 500);
  }
}

// --- Random Action Delay ---
// Затримка між окремими діями (betweenActions з SAFE_LIMITS)

export async function actionDelay(): Promise<void> {
  const { SAFE_LIMITS } = await import('./delays');
  await gaussianDelay(SAFE_LIMITS.betweenActions.min, SAFE_LIMITS.betweenActions.max);
}

// --- Lead Delay ---
// Затримка між лідами (betweenLeads з SAFE_LIMITS)

export async function leadDelay(): Promise<void> {
  const { SAFE_LIMITS } = await import('./delays');
  await gaussianDelay(SAFE_LIMITS.betweenLeads.min, SAFE_LIMITS.betweenLeads.max);
}
```
</action>

<acceptance_criteria>
- backend/src/utils/humanizer.ts містить `export async function humanType`
- Містить логіку помилок: `shouldMistake` і `page.keyboard.press('Backspace')`
- Містить `export async function humanScroll`
- Містить `page.mouse.wheel(` в humanScroll
- Містить `export async function actionDelay`
- Містить `export async function leadDelay`
</acceptance_criteria>
</task>

## Verification

```bash
# Перевірити всі exports humanizer
grep -n "^export async function" backend/src/utils/humanizer.ts
# Очікується: gaussianDelay, humanMouseMove, humanType, humanScroll, actionDelay, leadDelay

# Перевірити що Gaussian distribution реалізована (не Math.random() uniform)
grep "Math.sqrt" backend/src/utils/humanizer.ts
# Очікується: Math.sqrt(-2.0 * Math.log(u))

# Перевірити SAFE_LIMITS значення
grep "connectionRequestsPerDay" backend/src/utils/delays.ts
# Очікується: connectionRequestsPerDay: 20

grep "betweenLeads" backend/src/utils/delays.ts
# Очікується: betweenLeads: { min: 60_000, max: 180_000 }

# TypeScript компіляція
cd backend && npx tsc --noEmit
```

## Notes

- `gaussianDelay` використовує Box-Muller transform — це правильний підхід для нормального розподілу
- `humanType` не використовує `page.fill()` — це навмисно, fill() не імітує реальний ввід
- LinkedIn детектує `page.keyboard.type(fullText)` швидше ніж посимвольний ввід
- Відсоток помилок (3%) підібраний емпірично — не збільшувати, виглядає неприродньо
