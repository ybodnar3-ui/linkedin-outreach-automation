---
plan: "1.1"
title: "Project Setup + Database"
phase: 1
wave: 1
depends_on: []
autonomous: true
requirements_addressed: [INF-04, INF-05]
files_modified:
  - backend/package.json
  - backend/tsconfig.json
  - backend/src/index.ts
  - backend/src/services/storage.ts
  - backend/src/utils/logger.ts
  - backend/src/database/schema.sql
  - backend/src/database/migrations.ts
  - frontend/package.json
  - frontend/vite.config.ts
  - .gitignore
  - package.json
---

# Plan 1.1: Project Setup + Database

## Objective

Ініціалізувати TypeScript monorepo з backend (Express + SQLite) і порожнім frontend. Налаштувати логування. Визначити схему БД і запустити міграції. Після цього плану: `npm run dev` стартує backend на port 3001 без помилок.

## must_haves

```yaml
truths:
  - backend/src/index.ts існує і запускається без TS помилок
  - SQLite файл створюється при першому запуску (data/campaigns.db)
  - Всі 5 таблиць присутні в схемі: campaigns, campaign_steps, leads, daily_tracker, app_settings
  - Logger пише у logs/YYYY-MM-DD.log і в console
  - frontend/package.json існує з правильними залежностями
```

## Tasks

<task id="1.1.1">
<title>Monorepo root + .gitignore</title>

<read_first>
- README.md (поточний стан репозиторію)
</read_first>

<action>
Створити кореневий `package.json` з workspaces:

```json
{
  "name": "linkedin-outreach-automation",
  "private": true,
  "workspaces": ["backend", "frontend"],
  "scripts": {
    "dev:backend": "npm run dev --workspace=backend",
    "dev:frontend": "npm run dev --workspace=frontend"
  }
}
```

Створити `.gitignore`:
```
node_modules/
dist/
data/*.db
data/sessions/
logs/
.env
.env.local
```

Створити `backend/` і `frontend/` директорії.
</action>

<acceptance_criteria>
- package.json містить `"workspaces": ["backend", "frontend"]`
- .gitignore містить `data/*.db` і `data/sessions/`
- Директорії backend/ і frontend/ існують
</acceptance_criteria>
</task>

<task id="1.1.2">
<title>Backend: TypeScript + Express setup</title>

<read_first>
- .planning/REQUIREMENTS.md (секція INF)
- CLAUDE.md (секція Tech Stack)
</read_first>

<action>
Створити `backend/package.json`:

```json
{
  "name": "backend",
  "version": "1.0.0",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "better-sqlite3": "^9.6.0",
    "node-cron": "^3.0.3",
    "ws": "^8.17.0",
    "winston": "^3.13.0",
    "winston-daily-rotate-file": "^5.0.0",
    "multer": "^1.4.5-lts.1",
    "csv-parser": "^3.0.0",
    "playwright": "^1.44.0",
    "uuid": "^10.0.0",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/better-sqlite3": "^7.6.10",
    "@types/node": "^20.14.0",
    "@types/ws": "^8.5.10",
    "@types/multer": "^1.4.11",
    "@types/cors": "^2.8.17",
    "@types/uuid": "^10.0.0",
    "typescript": "^5.4.5",
    "ts-node-dev": "^2.0.0"
  }
}
```

Створити `backend/tsconfig.json`:
```json
{
  "compilerOptions": {
    "target": "ES2020",
    "module": "commonjs",
    "lib": ["ES2020"],
    "outDir": "./dist",
    "rootDir": "./src",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "resolveJsonModule": true
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist"]
}
```

Створити `backend/src/index.ts` (мінімальний Express сервер):
```typescript
import express from 'express';
import cors from 'cors';
import { initDatabase } from './services/storage';
import { logger } from './utils/logger';

const app = express();
const PORT = process.env.PORT || 3001;

app.use(cors());
app.use(express.json());

app.get('/api/health', (_req, res) => {
  res.json({ status: 'ok', timestamp: new Date().toISOString() });
});

async function start() {
  initDatabase();
  app.listen(PORT, () => {
    logger.info(`Backend running on port ${PORT}`);
  });
}

start().catch((err) => {
  console.error('Failed to start:', err);
  process.exit(1);
});
```
</action>

<acceptance_criteria>
- backend/package.json містить `"express"`, `"better-sqlite3"`, `"playwright"` в dependencies
- backend/tsconfig.json містить `"strict": true` і `"outDir": "./dist"`
- backend/src/index.ts містить `app.get('/api/health'`
</acceptance_criteria>
</task>

<task id="1.1.3">
<title>Logger (winston з ротацією)</title>

<read_first>
- CLAUDE.md (секція Data Models → Log Entry)
- .planning/REQUIREMENTS.md (INF-04)
</read_first>

<action>
Створити `backend/src/utils/logger.ts`:

```typescript
import winston from 'winston';
import DailyRotateFile from 'winston-daily-rotate-file';
import path from 'path';

const logDir = path.join(process.cwd(), 'logs');

const logFormat = winston.format.combine(
  winston.format.timestamp({ format: 'HH:mm:ss' }),
  winston.format.printf(({ timestamp, level, message }) => {
    const icon = level === 'error' ? '🔴' : level === 'warn' ? '⚠️' : level === 'info' ? '✅' : '📤';
    return `[${timestamp}] ${icon} ${message}`;
  })
);

export const logger = winston.createLogger({
  level: 'info',
  format: logFormat,
  transports: [
    new winston.transports.Console(),
    new DailyRotateFile({
      dirname: logDir,
      filename: '%DATE%.log',
      datePattern: 'YYYY-MM-DD',
      maxFiles: '30d',
      format: winston.format.combine(
        winston.format.timestamp(),
        winston.format.json()
      ),
    }),
  ],
});

export function logAction(params: {
  campaignId?: string;
  leadId?: string;
  action: string;
  message: string;
  level?: 'info' | 'warn' | 'error' | 'success';
  metadata?: Record<string, unknown>;
}) {
  const { level = 'info', message, ...meta } = params;
  logger.log(level === 'success' ? 'info' : level, message, meta);
}
```
</action>

<acceptance_criteria>
- backend/src/utils/logger.ts містить `DailyRotateFile` import
- Містить `maxFiles: '30d'`
- Містить `export function logAction`
- Містить `export const logger`
</acceptance_criteria>
</task>

<task id="1.1.4">
<title>SQLite схема і Storage service</title>

<read_first>
- CLAUDE.md (секція Data Models — Campaign, Lead, LogEntry)
- .planning/REQUIREMENTS.md (INF-05)
</read_first>

<action>
Створити `backend/src/services/storage.ts`:

```typescript
import Database from 'better-sqlite3';
import path from 'path';
import fs from 'fs';
import { logger } from '../utils/logger';

const dataDir = path.join(process.cwd(), 'data');
const dbPath = path.join(dataDir, 'campaigns.db');

let db: Database.Database;

export function getDb(): Database.Database {
  if (!db) throw new Error('Database not initialized. Call initDatabase() first.');
  return db;
}

export function initDatabase(): void {
  fs.mkdirSync(dataDir, { recursive: true });
  fs.mkdirSync(path.join(dataDir, 'sessions'), { recursive: true });

  db = new Database(dbPath);
  db.pragma('journal_mode = WAL');
  db.pragma('foreign_keys = ON');

  db.exec(`
    CREATE TABLE IF NOT EXISTS campaigns (
      id TEXT PRIMARY KEY,
      name TEXT NOT NULL,
      status TEXT NOT NULL DEFAULT 'paused',
      settings TEXT NOT NULL DEFAULT '{}',
      stats TEXT NOT NULL DEFAULT '{}',
      created_at TEXT NOT NULL DEFAULT (datetime('now')),
      updated_at TEXT NOT NULL DEFAULT (datetime('now'))
    );

    CREATE TABLE IF NOT EXISTS campaign_steps (
      id TEXT PRIMARY KEY,
      campaign_id TEXT NOT NULL REFERENCES campaigns(id) ON DELETE CASCADE,
      type TEXT NOT NULL,
      step_order INTEGER NOT NULL,
      config TEXT NOT NULL DEFAULT '{}',
      UNIQUE(campaign_id, step_order)
    );

    CREATE TABLE IF NOT EXISTS leads (
      id TEXT PRIMARY KEY,
      campaign_id TEXT REFERENCES campaigns(id) ON DELETE SET NULL,
      first_name TEXT NOT NULL,
      last_name TEXT NOT NULL,
      company TEXT NOT NULL DEFAULT '',
      title TEXT NOT NULL DEFAULT '',
      profile_url TEXT NOT NULL UNIQUE,
      status TEXT NOT NULL DEFAULT 'pending',
      current_step INTEGER NOT NULL DEFAULT 0,
      visited_at TEXT,
      connection_sent_at TEXT,
      connected_at TEXT,
      message_sent_at TEXT,
      replied_at TEXT,
      notes TEXT,
      tags TEXT DEFAULT '[]',
      error_message TEXT,
      created_at TEXT NOT NULL DEFAULT (datetime('now')),
      updated_at TEXT NOT NULL DEFAULT (datetime('now'))
    );

    CREATE TABLE IF NOT EXISTS daily_tracker (
      id TEXT PRIMARY KEY,
      date TEXT NOT NULL UNIQUE,
      connections_sent INTEGER NOT NULL DEFAULT 0,
      messages_sent INTEGER NOT NULL DEFAULT 0,
      profiles_visited INTEGER NOT NULL DEFAULT 0,
      searches INTEGER NOT NULL DEFAULT 0
    );

    CREATE TABLE IF NOT EXISTS app_settings (
      key TEXT PRIMARY KEY,
      value TEXT NOT NULL
    );

    CREATE INDEX IF NOT EXISTS idx_leads_campaign ON leads(campaign_id);
    CREATE INDEX IF NOT EXISTS idx_leads_status ON leads(status);
    CREATE INDEX IF NOT EXISTS idx_campaign_steps_campaign ON campaign_steps(campaign_id);
  `);

  logger.info('Database initialized at ' + dbPath);
}

// --- Daily Tracker ---

export function getTodayTracker() {
  const today = new Date().toISOString().split('T')[0];
  const db = getDb();
  let tracker = db.prepare('SELECT * FROM daily_tracker WHERE date = ?').get(today) as Record<string, unknown> | undefined;
  if (!tracker) {
    const { v4: uuidv4 } = require('uuid');
    db.prepare(
      'INSERT INTO daily_tracker (id, date) VALUES (?, ?)'
    ).run(uuidv4(), today);
    tracker = db.prepare('SELECT * FROM daily_tracker WHERE date = ?').get(today) as Record<string, unknown>;
  }
  return tracker;
}

export function incrementDailyCounter(field: 'connections_sent' | 'messages_sent' | 'profiles_visited' | 'searches'): void {
  const today = new Date().toISOString().split('T')[0];
  getTodayTracker(); // ensure exists
  getDb().prepare(`UPDATE daily_tracker SET ${field} = ${field} + 1 WHERE date = ?`).run(today);
}

// --- Settings ---

export function getSetting(key: string): string | null {
  const row = getDb().prepare('SELECT value FROM app_settings WHERE key = ?').get(key) as { value: string } | undefined;
  return row ? row.value : null;
}

export function setSetting(key: string, value: string): void {
  getDb().prepare('INSERT OR REPLACE INTO app_settings (key, value) VALUES (?, ?)').run(key, value);
}
```
</action>

<acceptance_criteria>
- backend/src/services/storage.ts містить `CREATE TABLE IF NOT EXISTS campaigns`
- Містить `CREATE TABLE IF NOT EXISTS leads`
- Містить `CREATE TABLE IF NOT EXISTS daily_tracker`
- Містить `CREATE TABLE IF NOT EXISTS app_settings`
- Містить `CREATE TABLE IF NOT EXISTS campaign_steps`
- Містить `export function initDatabase`
- Містить `export function getTodayTracker`
- Містить `export function incrementDailyCounter`
</acceptance_criteria>
</task>

<task id="1.1.5">
<title>Frontend: Vite + React + TailwindCSS skeleton</title>

<read_first>
- CLAUDE.md (секція Tech Stack → Frontend)
</read_first>

<action>
Створити `frontend/package.json`:

```json
{
  "name": "frontend",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.23.1",
    "@tanstack/react-query": "^5.45.0",
    "recharts": "^2.12.7",
    "@dnd-kit/core": "^6.1.0",
    "@dnd-kit/sortable": "^8.0.0",
    "lucide-react": "^0.394.0",
    "axios": "^1.7.2",
    "clsx": "^2.1.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.1",
    "typescript": "^5.4.5",
    "vite": "^5.2.13",
    "tailwindcss": "^3.4.4",
    "autoprefixer": "^10.4.19",
    "postcss": "^8.4.38"
  }
}
```

Створити `frontend/vite.config.ts`:
```typescript
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
  server: {
    port: 5173,
    proxy: {
      '/api': 'http://localhost:3001',
      '/ws': { target: 'ws://localhost:3001', ws: true },
    },
  },
});
```

Створити `frontend/tsconfig.json`:
```json
{
  "compilerOptions": {
    "target": "ES2020",
    "useDefineForClassFields": true,
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "react-jsx",
    "strict": true,
    "skipLibCheck": true
  },
  "include": ["src"]
}
```

Створити `frontend/tailwind.config.js`:
```javascript
/** @type {import('tailwindcss').Config} */
export default {
  content: ['./index.html', './src/**/*.{js,ts,jsx,tsx}'],
  theme: {
    extend: {
      colors: {
        surface: '#0f1117',
        'surface-2': '#1a1d27',
        'surface-3': '#252836',
        accent: '#2563eb',
      },
      fontFamily: {
        sans: ['Geist', 'system-ui', 'sans-serif'],
        mono: ['Geist Mono', 'monospace'],
      },
    },
  },
  plugins: [],
};
```

Створити `frontend/postcss.config.js`:
```javascript
export default {
  plugins: { tailwindcss: {}, autoprefixer: {} },
};
```

Створити `frontend/index.html`:
```html
<!doctype html>
<html lang="en" class="dark">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>LinkedIn Outreach Automation</title>
  </head>
  <body class="bg-surface text-white">
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

Створити `frontend/src/main.tsx`:
```tsx
import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <div className="min-h-screen bg-surface text-white flex items-center justify-center">
      <h1 className="text-2xl font-mono">LinkedIn Outreach Automation</h1>
    </div>
  </React.StrictMode>
);
```

Створити `frontend/src/index.css`:
```css
@tailwind base;
@tailwind components;
@tailwind utilities;

* { box-sizing: border-box; }
body { margin: 0; font-family: 'Geist', system-ui, sans-serif; }
```
</action>

<acceptance_criteria>
- frontend/package.json містить `"react"`, `"@dnd-kit/core"`, `"recharts"`, `"@tanstack/react-query"`
- frontend/vite.config.ts містить proxy для `/api` → `http://localhost:3001`
- frontend/tailwind.config.js містить `surface: '#0f1117'` і `accent: '#2563eb'`
- frontend/index.html існує
- frontend/src/main.tsx існує
</acceptance_criteria>
</task>

## Verification

```bash
# Встановити залежності
cd backend && npm install
cd ../frontend && npm install

# Запустити backend
cd ../backend && npm run dev
# Очікується: "Backend running on port 3001"
# Очікується: "Database initialized at .../data/campaigns.db"

# Перевірити health endpoint
curl http://localhost:3001/api/health
# Очікується: {"status":"ok","timestamp":"..."}

# Перевірити що logs/ директорія створилась і є .log файл
ls logs/

# Перевірити що БД містить всі таблиці
sqlite3 data/campaigns.db ".tables"
# Очікується: app_settings  campaign_steps  campaigns  daily_tracker  leads
```

## Notes

- `better-sqlite3` — синхронний API, не використовувати `await` для DB операцій
- `data/sessions/` директорія створиться автоматично при `initDatabase()`
- Logs ротуються через `winston-daily-rotate-file`, максимум 30 днів
